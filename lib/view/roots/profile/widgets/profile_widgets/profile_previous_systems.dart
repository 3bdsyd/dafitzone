import 'package:dafitzone/core/constants/app_package.dart';

class ProfilePreviousSystemWidget extends StatelessWidget {
  const ProfilePreviousSystemWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Lottie.asset(AppAnimation.emptyListAnimation);
  }
}
